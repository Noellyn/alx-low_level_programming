#include <stdio.h>

/**
*main - prints the alphabet in lower case
*Return: Always 0 (Success)
*/
int main(void)
{
char lc;

for (lc = 'a'; lc <= 'z'; ch++)
{
putchar(lc);
}
putchar('\n');
return (0);
}
