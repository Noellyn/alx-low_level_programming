#include <stdio.h>

/**
*main - prints all single digit numbers
*starting with 0
*Return: Always 0 (Success)
*/
int main(void)
{
int s;

for (s = 48; s < 58; s++)
{
putchar(s);
}
putchar('\n');
return (0);
}
