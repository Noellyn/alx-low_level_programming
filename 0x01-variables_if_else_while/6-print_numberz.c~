#include <stdio.h>

/**
*main - prints all single digit numbers
*starting with 0
*Return: Always 0 (Success)
*/
int main(void)
{
int s;

for (s = 0; s < 10; s++)
putchar(s);
putchar("\n");
return (0);
}
